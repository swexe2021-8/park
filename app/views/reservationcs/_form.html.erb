<%= form_with(model: reservationc, local: true) do |form| %>
  <% if reservationc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reservationc.errors.count, "error") %> prohibited this reservationc from being saved:</h2>

      <ul>
      <% reservationc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :start_time %>
    <%= form.datetime_select :start_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
